{% set version = "2.1.1" %}

package:
  name: pygrib
  version: {{ version }}

source:
  url: https://github.com/jswhit/pygrib/archive/v{{ version }}rel.tar.gz
  sha256: 8c1e7d9cb6ab283caa73654a7e655ab12d6c36e7c627e5d1766b4212071e6e70

build:
  number: 0
  skip: true  # [win and vc<14]

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - python
    - pip
    - cython
    - eccodes
    - jasper  # [not win]
    - jasper 2.0.14  # [win]
    - libpng
    - numpy
    - pyproj
    - zlib
  run:
    - python
    - setuptools
    - eccodes
    - jasper  # [not win]
    - jasper 2.0.14  # [win]
    - libpng
    - {{ pin_compatible('numpy') }}
    - pyproj
    - zlib

test:
  imports:
    - pygrib
  commands:
    - pip check
    - grib_repack -h
  requires:
    - pip

about:
  home: https://github.com/jswhit/pygrib
  license: MIT
  license_family: MIT
  license_file: COPYING
  summary: Python GRIB (editions 1 and 2) reader
  doc_url: https://jswhit.github.io/pygrib/
  dev_url: https://github.com/jswhit/pygrib

extra:
  recipe-maintainers:
    - dopplershift
    - jjhelmus
    - ocefpaf
